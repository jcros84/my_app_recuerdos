{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tema.id }}</td>
            </tr>
            <tr>
                <th>Tema</th>
                <td>{{ tema.tema }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ tema.CreatedAt ? tema.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tema_index') }}">back to list</a>

    <a href="{{ path('app_tema_edit', {'id': tema.id}) }}">edit</a>

    {{ include('tema/_delete_form.html.twig') }}
{% endblock %}
